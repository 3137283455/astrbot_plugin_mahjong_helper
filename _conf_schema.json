{
  "timezone": {
    "description": "每日何切使用的时区",
    "type": "string",
    "default": "Asia/Shanghai"
  },
  "state_dir": {
    "description": "SQLite 状态目录；留空时使用 AstrBot 的 data/plugin_data 目录",
    "type": "string",
    "default": ""
  },
  "enhance_images": {
    "description": "发送何切题图前生成高清 PNG 缓存，减少 QQ 压缩造成的模糊",
    "type": "bool",
    "default": true
  },
  "image_scale": {
    "description": "何切图片放大倍数（2-4，默认 2）",
    "type": "int",
    "default": 2
  },
  "koromo_token": {
    "description": "仅在牌谱屋维护者明确提供机器人可用的 API 凭据时填写；公开统计无需填写，网页临时 CAP 令牌不适用",
    "type": "string",
    "default": "",
    "obvious_hint": true
  },
  "records_limit": {
    "description": "最近对局命令返回的条数（1-20）",
    "type": "int",
    "default": 5
  },
  "subscribe_interval_4": {
    "description": "四麻订阅轮询间隔（秒，最低60）",
    "type": "int",
    "default": 180
  },
  "subscribe_interval_3": {
    "description": "三麻订阅轮询间隔（秒，最低60）",
    "type": "int",
    "default": 300
  }
}
