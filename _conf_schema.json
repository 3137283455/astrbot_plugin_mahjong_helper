{
  "media_priority": {
    "description": "用户没有明确说“看视频/听音频”时的默认媒体类型",
    "type": "string",
    "default": "video_first",
    "options": ["video_first", "audio_first", "video_only", "audio_only"],
    "labels": ["默认视频，可发送音频", "默认音频，可发送视频", "仅视频", "仅音频"],
    "hint": "默认值只影响未明确媒体类型的请求；“仅视频/仅音频”会拒绝相反类型的显式请求。"
  },
  "audio_form_priority": {
    "description": "音频未指定“直接播放/下载”时的优先形式",
    "type": "string",
    "default": "voice_first",
    "options": ["voice_first", "file_first"],
    "labels": ["优先语音，超限或平台不支持时发文件", "直接发音频文件"],
    "hint": "语音最长 15 分钟、25 MiB；文件最长不限、100 MiB。"
  }
}
